import { test } from '@playwright/test'

test.describe('auth login', () => {
  test.skip('admin puede ingresar al backoffice', async ({ page }) => {
    // Fase D — implementar con e2e/helpers/auth.ts
  })
})
